Before running `main.py` you need to install `tkinter` and `requests` libraries (if they are not pre-installed) in your Python3 environment.
If you have `pip` installed, just run:
```sh
pip install tk
pip install requests

```
